


<div class="container">
<div class="container d-flex p-0 m-0">
  <div class="card-trip big ml-0">
    <img class="big-image" src=<%= @flat.url %> />
    <div class="card-trip-infos">
      <div>
        <h2><%= @flat.name %></h2>
        <p><%= @flat.address %></p>
        <p><%= @flat.description %></p>
      </div>
      <h2 class="card-trip-pricing">$<%= @flat.price_per_night %></h2>
    </div>
  </div>
</div>
<%= link_to "Return to listings", flats_path, class:"btn btn-primary" %>
</div>
